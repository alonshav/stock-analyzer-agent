{
  "name": "agent",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/agent/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "webpack-cli build",
        "args": ["--node-env=production"],
        "cwd": "apps/agent"
      },
      "configurations": {
        "development": {
          "args": ["--node-env=development"]
        }
      }
    },
    "build:watch": {
      "executor": "nx:run-commands",
      "options": {
        "command": "webpack-cli build --watch --node-env=development",
        "cwd": "apps/agent"
      }
    },
    "prune-lockfile": {
      "dependsOn": ["build"],
      "cache": true,
      "executor": "@nx/js:prune-lockfile",
      "outputs": [
        "{workspaceRoot}/dist/apps/agent/package.json",
        "{workspaceRoot}/dist/apps/agent/package-lock.json"
      ],
      "options": {
        "buildTarget": "build"
      }
    },
    "copy-workspace-modules": {
      "dependsOn": ["build"],
      "cache": true,
      "outputs": ["{workspaceRoot}/dist/apps/agent/workspace_modules"],
      "executor": "@nx/js:copy-workspace-modules",
      "options": {
        "buildTarget": "build"
      }
    },
    "prune": {
      "dependsOn": ["prune-lockfile", "copy-workspace-modules"],
      "executor": "nx:noop"
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "dependsOn": ["build"],
      "options": {
        "buildTarget": "agent:build",
        "runBuildTargetDependencies": false,
        "watch": true,
        "inspect": false,
        "runtimeArgs": []
      },
      "configurations": {
        "development": {
          "buildTarget": "agent:build:development"
        },
        "production": {
          "buildTarget": "agent:build:production"
        }
      }
    }
  },
  "tags": ["type:app", "platform:node", "scope:agent"]
}
